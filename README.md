# emma.loquet.m1ecap

## Description
Ce package permet d'analyser les données de médailles des Jeux Olympiques.

## Données
Le jeu de données contient les résultats par discipline et par pays (médailles d'or, argent, bronze, total).

## Git

Le premier commit de ce répertoire a été effectué le 16/04/2026 à 09h20

### Exercice Git

Suite de commandes possible :

git checkout main
git commit -m "commit A"
git checkout -b feature
git commit -m "commit B"
git checkout main
git merge feature
